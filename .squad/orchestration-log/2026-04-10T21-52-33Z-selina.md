# Orchestration: Selina — Unresolve Feature (UI)

**Date:** 2026-04-10T21:52:33Z  
**Agent:** Selina (Frontend Dev)  
**Task:** Add Re-open button to PR comments XAML; wire `CanUnresolve` affordance.

## Outcome: Success ✅

### Deliverables

1. **PRReviewRemoteUserControl.xaml**
   - Added Re-open button after Resolve button (lines 437–439)
   - Button properties:
     - `Command="{Binding UnresolveCommand}"`
     - `Visibility="{Binding CanUnresolve, Converter={StaticResource BooleanToVisibleWhenTrueConverter}}"`
     - `ToolTip="Re-open this review thread"`
     - `Text="Re-open"`
     - `FlatListButtonStyle` + matching sizing

2. **PrCommentItem.cs**
   - Added `CanUnresolve` property with `[DataMember]` for VS extensibility
   - Enables/disables button visibility in XAML

3. **CommentThreadBuilder.cs**
   - Updated `Build` signature: accepts `createUnresolveCommand` factory
   - Sets `CanUnresolve = true` when:
     - `UnresolveCommand` is not null
     - `threadState` is not null
     - Thread **IS** resolved (`threadState.IsResolved == true`)
     - Valid `ReviewThreadId` exists

### Visibility Logic

**Only one button visible at a time:**
- Thread unresolved → Show Resolve, hide Re-open
- Thread resolved → Hide Resolve, show Re-open
- No thread ID → Hide both

### Files Modified

- `Diffinitely/ToolWindows/PRReviewRemoteUserControl.xaml`
- `Diffinitely/Models/PrCommentItem.cs`
- `Diffinitely/ToolWindows/CommentThreadBuilder.cs`

### Testing

✅ `dotnet build` — no errors  
✅ All 23 tests pass — `dotnet test`  
✅ Manual end-to-end deferred to Renee

### Pattern Notes

- Mirrors existing Resolve pattern exactly
- Capability property drives visibility
- Command wiring via factory function
- ViewModel passes dependencies
- XAML binds to capability + command
- Post-action reload preserves filters

---

**Status:** Ready for integration  
**Next:** Renee validates full end-to-end flow and button state transitions.
