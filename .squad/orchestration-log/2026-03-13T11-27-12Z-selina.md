# Orchestration Log — Selina (2026-03-13)

**Issue:** #10 — Resolve button in comments view  
**Authority:** Bruce (Lead) — Design approved for execution  
**Timestamp:** 2026-03-13T11:27:12Z

## Assigned Scope

**UI/ViewModel Owner** for issue #10 resolution feature.

### Owned Files

- `Diffinitely\ToolWindows\PRReviewViewModel.cs`
- `Diffinitely\ToolWindows\PRReviewRemoteUserControl.xaml` (visibility binding only)

### Responsibilities

1. Populate `ReviewThreadId` into each `PrCommentItem` during model construction
2. Wire `ResolveCommand` for comment items that have a valid thread ID
3. Remove the unsafe file+line regrouping pass that can collapse separate threads into one UI item
4. Ensure comments UI never presents a clickable no-op action (visibility gated on command availability)
5. Implement post-resolve refresh/reload so comments list, filters, and button visibility update from server truth

### Execution Order

1. Wait for Lucius to complete `ResolveCommand` and thread-ID model updates
2. Receive thread ID in model; populate into `PrCommentItem` in ViewModel
3. Remove file+line regrouping logic from comment assembly
4. Wire `ResolveCommand` during comment list construction
5. Implement refresh handler post-resolve
6. Coordinate with Renee on refresh/filter correctness

### Architecture Guardrails

- **No false affordances** — button visibility must track command availability (hide if no thread ID)
- **No thread merging by file+line** — remove grouping pass that can hide thread identity
- **Guaranteed consistency post-action** — after resolve succeeds, reload from GitHub to ensure filters, button state, and item visibility are server-authoritative

### Coordination

- **Lucius (Backend):** Will provide `ReviewThreadId` in model and implement `ResolveCommand`
- **Renee (Tester):** Will validate refresh/filter behavior and test wiring
- **Bruce (Lead):** Design authority
