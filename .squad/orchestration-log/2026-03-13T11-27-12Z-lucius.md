# Orchestration Log — Lucius (2026-03-13)

**Issue:** #10 — Resolve button in comments view  
**Authority:** Bruce (Lead) — Design approved for execution  
**Timestamp:** 2026-03-13T11:27:12Z

## Assigned Scope

**Service/Backend Owner** for issue #10 resolution feature.

### Owned Files

- `Diffinitely\Services\GitHubPullRequestService.cs`
- `Diffinitely\Models\PullRequestInfo.cs`
- `Diffinitely\Models\PrCommentItem.cs` (thread identity fields only)
- `Diffinitely\Commands\ResolveCommand.cs` (new)

### Responsibilities

1. Add review thread node ID retrieval to GraphQL query for `GetReviewThreadResolutionAsync`
2. Extend `PullRequestInfo` to store explicit thread metadata (thread node ID) keyed by review comment database ID
3. Add `ReviewThreadId` field to `PrCommentItem` model
4. Implement `resolveReviewThread` GraphQL mutation method that handles transport/API failures gracefully
5. Create `ResolveCommand` class implementing `IAsyncCommand` interface
6. Ensure the command is impossible to execute without a valid thread ID

### Execution Order

1. Extend GraphQL query to capture thread node IDs
2. Update models (`PullRequestInfo`, `PrCommentItem`) with thread identity fields
3. Implement resolve mutation method in `GitHubPullRequestService`
4. Create `ResolveCommand` class
5. Coordinate with Selina on command wiring in `PRReviewViewModel`

### Architecture Guardrails

- **Service is source of truth** for thread actions; all resolver mutations go through service
- **No silent failures** — if mutation fails, item does not flip to resolved locally; extension remains stable
- **Thread ID plumbing is required** — resolve action only exists when thread ID is present
- **Large PR pagination risk acknowledged** — current query uses `first:100` with no pagination; known follow-up

### Coordination

- **Selina (Frontend):** Will wire `ResolveCommand` in ViewModel after command is complete
- **Renee (Tester):** Will cover success/failure/refresh behavior with regression tests
- **Bruce (Lead):** Design authority; all critical decisions bubble up
