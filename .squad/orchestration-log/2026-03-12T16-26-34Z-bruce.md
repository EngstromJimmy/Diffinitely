# Orchestration: Bruce Investigation Completion (2026-03-12T16:26:34Z)

**Agent:** Bruce (Lead)  
**Task:** Resolve button in PR comments view — full investigation  
**Status:** Complete  
**Duration:** Investigation phase complete; recommendation documented

## Artifacts Delivered

| Artifact | Location | Status |
|----------|----------|--------|
| Investigation Report | `.squad/decisions/inbox/bruce-resolve-button-investigation.md` | ✓ Written |
| History Update | `.squad/agents/bruce/history.md` | ✓ Updated |

## Key Findings

1. **Resolve button is wired in XAML** but the backing command is **never instantiated**.
2. **No `ResolveCommand.cs` exists** — only `OpenDiffCommand` and `OpenForReviewCommand` in `/Commands/`.
3. **Data model gap:** `PrCommentItem` lacks `ReviewThreadId` — GitHub API requires thread ID for resolve mutation.
4. **Backend available:** GitHub GraphQL `resolveReviewThread` mutation is stable and ready.
5. **Root cause:** Incomplete feature — planned but never implemented.

## Recommended Fix Path

**Phase 1:** Add `ReviewThreadId` to `PrCommentItem`; extend `GetReviewThreadResolutionAsync` to capture thread IDs.  
**Phase 2:** Create `ResolveCommand` implementing `IAsyncCommand` with GraphQL resolve mutation.  
**Phase 3:** Wire command in `PRReviewViewModel` and test end-to-end.  

**Risk:** Low. No existing code depends on this unimplemented feature.

## Next Steps for Team

- Decide: Ship with hidden Resolve button or implement Phase 1–3 fix.
- If implementing: Prioritize Phase 1 (data model) to unblock testing.
- Add automated tests for comment action wiring per Renee's acceptance bar.

## Handoff

Scribe now merging inbox decisions and documenting team consensus.
